import { ContactsList } from '../../../widgets/contactsList';
import { Note } from '../../../widgets/note';

export const HomePage = () => {
  return (
    <>
      <ContactsList />
      <Note />
    </>
  );
};
