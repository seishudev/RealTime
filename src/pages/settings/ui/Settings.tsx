import { ProfileCustom } from '../../../widgets/profileCustom';
import cl from './Settings.module.scss';

export const Settings = () => {
  return (
    <div className={cl.container}>
      <ProfileCustom />
    </div>
  );
};
